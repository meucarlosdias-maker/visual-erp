import { ProductionPanel } from '@/modules/projects/components/ProductionPanel';

export const metadata = {
  title: 'Painel dos Setores | Visual ERP',
};

export default function ProductionPanelPage() {
  return <ProductionPanel />;
}
